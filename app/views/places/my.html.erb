<div class="container">
	<% if @places.count == 0 %>
	  <div class="jumbotron">
	    <h1>Are you new?</h1>
	    <h3>You don't have any place created, your adventure begin here.</h3>
	    <p align="center">
	      <%= link_to '<i class="fa fa-plus"></i> New Place'.html_safe, new_place_path, class: "btn btn-primary btn-lg" %>
	    </p>
	  </div>
	<% else  %>
		<% @places.each do |place| %>
			<%= render "card", place: place %>
		<% end %>
	<% end %>
</div>